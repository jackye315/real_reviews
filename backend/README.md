# Real Reviews API

Container-only FastAPI backend. Prefer the root `Makefile` targets, e.g. `make api-test`, `make api-lint`, and `make migrate`.

Direct Compose commands should be run from the repository root with `-f docker/compose.yaml -f docker/compose.override.yaml`.
